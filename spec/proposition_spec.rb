require 'spec_helper'

describe Proposition do
  it 'has a version number' do
    expect(Proposition::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
